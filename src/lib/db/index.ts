/**
 * Cloudflare D1 — gunakan binding via getRequestContext() pada Route Handlers / Pages.
 * Schema & query helpers ditambahkan saat fitur SaaS (hygiene, analytics, auth) diimplementasi.
 */
export {};
