import type { Locale } from "@/lib/i18n/locale";
import type { FaqItem } from "@/types";

/** FAQ copy akan diisi di dictionaries saat section FAQ diimplementasi. */
export function getFaqItems(_locale: Locale): FaqItem[] {
  return [];
}
